<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nutrix • Home</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            background: #0b0b0b;
            font-family: Arial, Helvetica, sans-serif;
            color: #fff;
        }

        /* Cabeçalho */
        header {
            background: #0f0f0f;
            padding: 20px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 2px solid #00d455;
        }

        header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: bold;
            color: #00ff66;
        }

        .menu a {
            margin-left: 25px;
            text-decoration: none;
            color: #eee;
            font-weight: bold;
            padding: 8px 12px;
            border-radius: 6px;
            transition: 0.3s;
        }

        .menu a:hover {
            background: #00ff6633;
        }

        /* Conteúdo */
        .container {
            text-align: center;
            padding: 70px 20px;
        }

        .container h2 {
            font-size: 38px;
            color: #00ff66;
            margin-bottom: 10px;
        }

        .container p {
            font-size: 17px;
            color: #cfcfcf;
            max-width: 600px;
            margin: 0 auto 30px;
        }

        /* Botões */
        .btn {
            display: inline-block;
            padding: 14px 28px;
            margin: 12px;
            border-radius: 10px;
            font-size: 18px;
            text-decoration: none;
            font-weight: bold;
            color: #fff;
            background: #00cc55;
            border: 2px solid #00ff66;
            transition: 0.25s;
        }

        .btn:hover {
            background: #00ff66;
            transform: scale(1.05);
        }

        /* Rodapé */
        footer {
            text-align: center;
            padding: 20px 0;
            background: #0f0f0f;
            color: #777;
            font-size: 14px;
            margin-top: 50px;
            border-top: 2px solid #00d455;
        }
    </style>
</head>

<body>

    <header>
        <h1>Nutrix</h1>
        <nav class="menu">
            <a href="#">🏠 Home</a>
            <a href="#">📅 Consultas</a>
            <a href="#">👤 Clientes</a>
            <a href="#">🩺 Nutricionistas</a>
        </nav>
    </header>

    <div class="container">
        <h2>Bem-vindo ao Nutrix</h2>
        <p>Gerencie consultas, clientes e planos alimentares de forma simples e eficiente.</p>

        <a href="#" class="btn">👤 Cadastrar Cliente</a>
        <a href="#" class="btn">📅 Agendar Consulta</a>
        <a href="#" class="btn">🍎 Planos Alimentares</a>
    </div>

    <footer>
        © 2025 Nutrix — Todos os direitos reservados.
    </footer>

</body>
</html>
